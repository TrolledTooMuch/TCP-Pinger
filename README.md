```python
usage: main.py [-h] -p  [-t]

positional arguments:
                   Host/IP the TCP ping goes to.  

optional arguments:
  -h, --help       show this help message and exit
  -p , --port      The TCP ping port.
  -t , --timeout   Timeout of the ping connection.
 ```
